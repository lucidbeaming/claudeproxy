# Port this proxy listens on
PORT=8082

# LM Studio base URL (default is LM Studio's default)
LM_STUDIO_URL=http://127.0.0.1:1234

# Optional: override whatever model name Claude sends with a specific LM Studio model ID
# MODEL_OVERRIDE=lmstudio-community/Meta-Llama-3-8B-Instruct-GGUF
